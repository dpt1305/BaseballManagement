import React from 'react';
import {SafeAreaView, Text} from 'react-native';

export default function AddMemberScreen({navigation}) {
  return (
    <SafeAreaView>
      <Text>Add memeber screen</Text>
    </SafeAreaView>
  );
}
